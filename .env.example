# Optional local-only values. Do not commit real secrets.
# Cloudflare Pages Functions use the D1 binding named DB.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=

# Email auto-promoted to super admin on first login. Set this as a Pages
# environment variable in production. No default: if unset, no super admin exists.
SUPER_ADMIN_EMAIL=
